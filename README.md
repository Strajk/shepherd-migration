# Migrations for my repos

See https://github.com/NerdWalletOSS/shepherd for info.
See [shepherd.yml](./shepherd.yml) for rules.

* Run `(npm run)` `checkout`
* Run `(npm run)` `apply`
* Run `(npm run)` `open-finder` or `open-webstorm`
* 👀 Verify changes 👀
* Run `(npm run)` `commit`
* Run `(npm run)` `push`
* Run `(npm run)` `pr`
